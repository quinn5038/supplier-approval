# 本地修复与测试报告

日期：2026-09-13。仓库：quinn5038/supplier-approval。审查基线：2455f18。交付分支：main。

后续按用户操作偏好恢复 BAT 启动与网页粘贴 Authorization/Cookie。默认无需额外本地账号，限制本机连接并保留 CSRF/Host 检查；网页凭证仅更新内存，不保存或备份。账号模式改为可选。该阶段新增 3 项回归用例，总计 62 项通过；浏览器使用合成凭证验证弹窗提交和待办刷新，未调用真实平台。

配置跟进（2026-09-13）：用户在本机 `.env` 填写了 TextIn 两项开发者凭证，仅检查非空；用程序生成的 `TEST 12345` 图片调用 TextIn 成功，未外发供应商材料。隔离安装 PaddlePaddle 2.6.2/PaddleOCR 2.9.1 和官方三组模型；中文项目路径在 Paddle C++ 加载阶段不被支持，现仅复制公开模型到 ASCII 临时目录，随后断网运行真实识别和无法识别时全黑检查成功。平台 APP_TOKEN、AGENT_ID 仍未提供；CODE 已确认未参与请求并从配置界面移除。企查查内部数据接口无法连接，相关项保留人工核验。该阶段回归为 63 项通过。

首次安装器补充：新增 `installer/` 构建脚本和单文件 Windows EXE（`dist/SupplierApproval-Setup.exe`），内含源码白名单，不含 `.env` 和用户缓存；运行时从 python.org 获取经官方 Sigstore 摘要固定校验的 3.12.10 安装包，分别安装主项目和 PaddleOCR 依赖、模型，执行断网合成图识别自检。安装目录默认当前用户的 LocalAppData；需要联网且目前未做企业代码签名。完整安装测试使用当前机器已有 Python3.12 作入口模拟“Python 已安装”路径；官方下载/静默安装分支仅用官方文档和下载摘要核对，尚未在全新无 Python Windows 机器实测。

安装器验收：该阶段 67 项 pytest 回归通过（含安装包清单、路径穿越、Python 下载摘要拒绝用例），Ruff/编译与 `git diff --check` 通过；在隔离目录从空目录完整部署、安装双环境和三组模型、断网实测真实 OCR 推理；再次运行同一 EXE 继续安装、生成空白 `.env`、保留本地安装标记。安装后演示 Web 返回 200、3 条合成待办、健康检查 OK。测试中使用现有本地 Python 3.12 作为安装入口，不代表首次下载/静默安装 Python 分支经过新机器实测。当前重新构建的 EXE `--check-bundle` 验证 35 个文件、无 `.env` 和用户缓存；SHA-256：`c7a1bf3b05db4260784ade3ff71538781b8c32e518a2233f3726fc994d823b8b`。

本次依据用户提供的评审文档核对并修复代码问题。文档中的评分、获奖预测和建议不视为已证实事实。用户已另行授权推送；未调用真实审批写入。

## 问题与落地改动

| 评审问题 | 本次处理 | 主要位置 |
|---|---|---|
| 财报在废弃提取器返回前已经外发；混合敏感材料风险 | 当前材料元数据允许名单与文件名、类型描述敏感标记共同限制；传输入口再次校验，财报、身份证、未知和混合材料不进入 TextIn | material_policy.py、textin_pipeline.py |
| 脱敏异常回退原始文件 | 脱敏异常停止或转人工；公开暂存目录只接收允许材料；不再复制原始证件 | desensitize.py、textin_pipeline.py |
| 身份证预览可能泄露、PDF 多页处理不完整 | 预览需本次成功打码文件哈希；全页无法识别时遮黑；卡片外遮黑；合并多页字段，预览仍为第一页 | offline_desens/idcard_masker.py、web/app.py |
| 缺依赖、失效自测、Windows 路径 | 补齐 requests 和 multipart 等依赖，增加独立安装脚本与 CI；修复废弃财务用例；自测状态使用 ASCII 避免 GBK 崩溃；移除输出及模型环境的个人绝对路径 | requirements*.txt、setup.ps1、textin_pipeline.py |
| 审批硬编码 P0702、回写保护不足 | 显式单据类型、Pydantic 标识模型、完整载荷确认摘要、实时任务状态和按钮校验、幂等键；写请求不自动重试；Web 强制模拟 | approval_guard.py、auto_approve.py、state_store.py |
| Web 访问边界、监听全部网卡、明文 Cookie 备份 | 默认本机免额外登录，CSRF/Origin/Host 与连接来源检查；可选账号模式；网页粘贴凭证只存内存，删除文件保存与备份路径 | web/security.py、web/app.py、启动脚本及页面 |
| 同类多附件覆盖 | 保留 documents，全部通过才可通过，失败或未知不被后项覆盖 | material_policy.py、textin_pipeline.py |
| FETCH_ONLY 跳过已缓存记录、OCR 结果陈旧 | 指定任务强制刷新元数据和附件，下载失败中止；OCR 按内容哈希复用；移除的附件与旧结果不再混入 | auto_approve.py、textin_pipeline.py |
| 企查查财务返回格式不匹配 | 识别嵌套报表期间与旧结构，记录公开来源；公开数据不足以替代三表及审计意见，一律保持财务人工核验；企业主体信息不完整也不能自动通过 | financial_data.py、qcc_api_client.py、auto_approve.py |
| 港澳台分流、空清单及最终意见不一致 | 港澳台统一人工核验；空清单不建议通过；驳回决策与最终意见一致；OCR 不覆盖企业信息冲突 | auto_approve.py、gen_opinion.py |
| 缓存删除顺序错误、旧报告误展示 | 删除前获取文件统计并限制路径；刷新中或失败时不返回旧成功报告 | textin_pipeline.py、web/app.py |
| 状态与审计薄弱 | Web 任务与企查查缓存改用 SQLite；任务重启恢复；外发和审批事件带哈希链，触发器禁止更新、删除；企查查缓存有 TTL | state_store.py、qcc_api_client.py、web/app.py |
| 死代码、页面信息与工程交付不足 | 删除废弃财务主体与旧报告重复实现，修复 Ruff 问题；增加搜索筛选、演示、配置检查、转义、安装和 CI 说明 | 报告模块、web/、demo.py、preflight.py、README.md |

## 本地验证结果

在 Windows 独立 `.venv` 中从 `requirements-dev.txt` 安装，使用合成数据及模拟接口。pytest 阻断外网，只允许测试框架所需回环连接。

| 检查 | 结果 |
|---|---|
| `python -m pytest -q` | 69 passed；2 个上游弃用警告 |
| `python textin_pipeline.py test` | 内置 19 项检查通过；Windows 默认编码下不再异常退出 |
| `python -m pip check` | No broken requirements found |
| `python -m ruff check .` | E4/E7/E9/F/I 全部通过 |
| `python -m compileall -q -x "[.]venv|[.]test-deps" .` | 通过 |
| `git diff --check` | 通过；仅 Windows 换行转换提示 |
| 浏览器手工验收 | 本地演示登录、3 类决策显示、搜索过滤和人工核验详情可用；测试服务已停止 |

回归覆盖：敏感类型、混合材料、敏感描述、外发入口、脱敏异常、缓存内容变更、附件移除、多附件聚合、批准前状态变化与缺少按钮、确认摘要、重复回写、匿名访问、只读角色、会话到期、CSRF、Host、限流、注册资本边界、规则表达式约束、报告 HTML 转义、港澳台分流、财务不完整、图像全黑兜底及实际双页 PDF 输出。

图像/PDF 回归测试调用真实 OpenCV、PyMuPDF、NumPy 图像处理，OCR 结果使用替身；另用合成图片断网运行真实 Paddle 模型自检。浏览器验收使用合成演示数据，并非真实业务端到端验收。

两个警告分别来自 Starlette 对 httpx TestClient 的弃用提示、AnyIO BlockingPortal 别名弃用提示；不影响此次测试通过，后续升级测试依赖时应处理。

## 尚需外部条件或后续改造

1. 未使用真实平台及企查查凭据，TextIn 仅用合成图片测试；未进行真实审批写入、供应商材料外发或生产数据测试。真实接口契约、网络错误行为与实际权限仍需在公司测试环境验收。
2. 已安装并以合成图片运行真实 PaddleOCR 模型；运行时仍要求本地模型就绪，缺失时停止或转人工。真实低清、反光及复杂证件样本的识别与脱敏效果仍需授权样本验收。
3. 文档建议的完整五层重构、全部业务 JSON 向事务存储迁移及多实例并发支持未在此次增量修复中完成。当前仍要求单 Web worker，避免与独立批处理并发执行。SQLite 审计无法阻止主机管理员替换数据库。
4. 公开材料类型标签不能代替保密分类，财务核验仍需人工。预检只检查配置就绪度，不代表外部服务实时健康。
5. 盲测准确率、耗时基准、演示视频及开源许可证需要授权数据、实际测量或权利人决定，本次没有虚构这些结果，也未擅自添加许可证。

当前结论：上述本地自动化检查范围内未发现回归；不能据此保证生产环境无异常。
