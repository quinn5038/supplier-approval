# Python 依赖清单（v1.3）
#
# 最小化运行（只需要以下）：
# - Web UI：FastAPI + Jinja2 + Uvicorn
# - 规则引擎配置：PyYAML
#
# 9/9 简化：移除财报 PDF 脱敏（PyMuPDF/pdfplumber）和粗比例身份证打码（Pillow），
# 身份证脱敏改由外部 PaddleOCR 脚本（idcard_masker.py，独立 Python 3.11 环境）负责，
# 财报按保密合规要求不通过 OCR 解析，改走企查查/人工核验。

# Web 服务
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# 数据/配置
PyYAML>=6.0

# 标准库自带但列出来方便对照：
# - json, re, logging, pathlib, urllib, hashlib, threading, subprocess, datetime
