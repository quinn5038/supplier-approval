# ICCEC 供应商智能审批系统 · 竞赛提交说明

> 港湾工程 2026 AI+ 竞赛（D 类）参赛作品

## 一、项目简介

面向中国港湾工程有限责任公司供应链管理部的**供应商准入智能审批系统**。依据《中港采购发〔2025〕161 号》管理办法，将「拉取待办 → 下载资质 → 离线脱敏 → OCR 识别 → 规则判定 → 生成报告」全流程自动化，把人工逐家核验供应商资质的工作，收敛为「一键后台审批 + 人工复核结论」。

## 二、解决的问题

供应商准入审批长期依赖人工逐家下载并核对营业执照、法人身份证、ISO 三认证、纳税信用、财报等十余类资质文件，存在三大痛点：

1. **效率低**：单家供应商需人工核对十余项材料，耗时长、易疲劳漏检；
2. **合规风险**：身份证、财报等敏感文件若直接外发 AI 处理，存在数据泄露风险；
3. **标准难统一**：准入标准分散在制度文件中，人工判断尺度不一，且存在串标围标等人为因素空间。

本系统将准入标准**量化、配置化、可复现**，用 AI 完成重复性核验，人工只做关键判断复核。

## 三、技术方案

### 架构

```
┌─────────────────────────────────────────────────┐
│  Web UI（FastAPI + Jinja2 + Uvicorn，端口 8000） │
│    首页待办列表 · 后台审批 · 进度追踪 · 报告预览   │
└───────────────────────┬─────────────────────────┘
                        │ subprocess 串接
        ┌───────────────┼────────────────────────┐
        ▼               ▼                        ▼
  拉取待办          OCR 识别                   规则判定
  auto_approve.py  textin_pipeline.py       rules.yaml
  (scpma 接口)     (TextIn + PaddleOCR)     (准入标准配置)
        │               │                        │
        ▼               ▼                        ▼
  下载资质          离线脱敏                  生成报告
  (files_cache)    desensitize.py          gen_stage2_report.py
                   (PaddleOCR 身份证         + gen_opinion.py
                    精确打码)                (HTML + 审批意见)
```

### 核心流程

1. **拉取待办**：调用 scpma.iccec.cn 接口获取待办供应商列表；
2. **下载材料**：自动下载每家供应商上传的营业执照/身份证/财报/证书等附件；
3. **离线脱敏**（合规核心）：身份证用 PaddleOCR 本地精确打码，只保留「姓名+有效期限」，敏感信息（证号/住址/头像）不出本机；财报按保密合规不 OCR 解析，改走企查查/人工核验；
4. **OCR 识别**：TextIn 识别营业执照/ISO/纳税信用等证照，抽取关键字段；
5. **规则判定**：按 rules.yaml 中的 161 号文准入标准逐项判定（通过/退回/转人工）；
6. **生成报告**：输出 HTML 审查报告 + 审批意见纯文本，人工复核后填入审批框。

## 四、核心亮点

| 亮点 | 说明 |
|---|---|
| **离线脱敏（保密合规）** | 身份证用飞桨 PaddleOCR 在本地离线识别文字框坐标、精确打码，敏感信息根本不外发，从源头规避数据泄露风险 |
| **离线脱敏方案可复用（潜力）** | 该方案可独立抽取为通用脱敏组件，扩展接入驾驶证、护照、营业执照关键字段等其他证件，供全公司（尤其商法等证件较多的部门）在合规调用 AI 前本地脱敏敏感信息，降低证据信息泄露风险 |
| **可复现、可移交** | 不绑定个人连接器；版本走 GitHub；依赖钉版本；路径/凭据环境变量化，换机器一条命令复现 |
| **规则引擎 YAML 化** | 161 号文 13 项通用准入 + 厂家/贸易商/服务商分类规则全部落成 YAML 配置，标准可读可改可审计 |
| **端到端自动化闭环** | 从拉取到报告一键完成；单家审批耗时由数十分钟降至 1 分钟以内（含 PaddleOCR 单家打码优化） |
| **多类型材料智能核验** | 支持一个上传文件对应多种材料（如"财报+纳税信用+售后"合并件）的拆分核验 |

## 五、技术栈

- **后端**：Python 3.13 · FastAPI · Uvicorn · Jinja2
- **OCR**：TextIn xParse（证照识别）+ 飞桨 PaddleOCR（身份证离线打码/识别）
- **脱敏**：飞桨 PaddleOCR（身份证离线精确打码）
- **规则**：PyYAML（rules.yaml 配置化）
- **数据核验**：企查查（商业信誉/财务）· 天眼查（系统兜底）
- **版本管理**：Git · GitHub

## 六、复现步骤

### 1. 主项目环境（Python 3.13）

```bash
pip install -r requirements.txt        # FastAPI/Jinja2/Uvicorn/PyYAML
```

### 2. 配置凭证（.env）

```bash
cp .env.example .env   # 填入 SCPMA_COOKIE / TEXTIN_APP_ID / TEXTIN_SECRET_CODE
```

### 3. PaddleOCR 离线脱敏环境（独立 Python 3.11）

```bash
winget install Python.Python.3.11
"C:\...\Python311\python.exe" -m venv idcard_env
idcard_env\Scripts\python.exe -m pip install paddlepaddle==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
idcard_env\Scripts\python.exe -m pip install paddleocr==2.9.1 -i https://pypi.tuna.tsinghua.edu.cn/simple
idcard_env\Scripts\python.exe -m pip install opencv-python pymupdf pillow
```

### 4. 启动 Web UI

```bash
uvicorn web.app:app --host 0.0.0.0 --port 8000
```

浏览器打开首页 → 更新 Cookie → 点「后台审批」，系统自动跑完整流水线，完成后点「查看结果」看审查报告。

> 详细文档见项目根目录 `README.md`（含 PaddleOCR 环境的三个关键踩坑、环境变量表等）。

## 七、实际效果

- 已覆盖 **40 家**供应商的完整审批流程（拉取→脱敏→OCR→判定→报告）；
- 身份证敏感信息 **100% 本地脱敏**后才参与处理，财报/身份证不走云端 OCR；
- 单家供应商审批由人工数十分钟收敛到 **1 分钟内自动完成**，人工仅做最终结论复核。

## 八、现状说明与后续规划

- **商业信誉核验现状**：目前对企业商业信誉的核验（失信被执行人、被执行人、限制消费令、经营异常、严重违法等），因接入企查查免费版接口，尚无法实现自动化核验；该数据接口已向集团提交申请，目前处于审批阶段，审批通过后即可在程序中实现上述信息的自动化核验。
